name: CI
run-name: CI Build

on:
  push:
    branches: ['create-ci-workflow']
  pull_request:
    branches: ['main']

permissions:
  id-token: write
  contents: read    #allow code checkout

# Cancel currently running workflow from the same PR or branch
concurrency:
  group: ${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build_push:
    name: Build
    runs-on: ubuntu-latest
    environment: central

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: 17

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.10"

      - name: Run Gradle Wrapper
        run: gradle wrapper

      - name: Build with Gradle
        run: ./gradlew clean build -x test

      - name: See what we have
        if: always()
        run: ls -lah

      - name: Configure AWS Credentials for image push
        uses: aws-actions/configure-aws-credentials@v4
        with:
          audience: sts.amazonaws.com
          aws-region: ap-southeast-2
          role-to-assume: ${{ vars.AODN_CENTRAL_GITHUB_ACTIONS_ROLE }}

      - name: Login to ECR Central
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ vars.OCEANCURRENT_IMAGE_URI }}

  trigger_edge_deploy:
    needs: [build_push]
    uses: ./.github/workflows/trigger_deploy.yml
    with:
      app_name: oceancurrent
      environment: edge
      digest: ${{ needs.build_push.outputs.digest }}
    secrets: inherit